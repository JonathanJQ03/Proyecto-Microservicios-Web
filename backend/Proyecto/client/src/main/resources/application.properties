spring.application.name=client
# Puerto del servidor HTTP
server.port=${PORT:8088}

# === Configuración de la base de datos PostgreSQL ===
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_DATABASE:clients_db}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:admin123}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Mostrar SQL en consola (opcional)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# === Seguridad con OAuth2 ===
# spring.security.oauth2.resourceserver.jwt.issuer-uri=http://oauth2-server:9000
# spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8081/.well-known/jwks.json
# spring.security.oauth2.resourceserver.jwt.jwk-set-uri= # NO usar
spring.security.oauth2.resourceserver.jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970






